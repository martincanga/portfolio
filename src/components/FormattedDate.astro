---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString("es-es", {
      month: "short",
      day: "numeric",
      year: "numeric",
    })
  }
</time>
